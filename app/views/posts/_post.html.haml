= div_for post do
  #blog_post
    .blog_info_top
      = image_tag post.user.avatar.url(:thumb)
      %h2= link_to_unless_current h(post.title), post
      %h3
        by: 
        = post.user
    .blog_content
      = post.body.html_safe
