#navigation
  %ul.nav
    = tab('/', :home)
    = tab('/about', :about)
    = tab('/services', :services)
    = tab('/blog', :blog)
    = tab('/contact', :contact)
    = tab('/community', :community)
    = tab('/home/payments', :payments)

